apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: chaos-lab
resources:
  - ../../base
patches:
  - target:
      kind: Deployment
      name: chaos-lab-api
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
  - target:
        kind: HorizontalPodAutoscaler
        name: chaos-lab-api
    patch: |-
        - op: replace
          path: /spec/maxReplicas
          value: 1
